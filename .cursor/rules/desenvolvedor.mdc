---
alwaysApply: true
---
---
description: "Desenvolvimento Full-Stack com Java, Spring Boot, Thymeleaf e Bootstrap"
globs: ["*.java", "*.html", "*.js", "*.css"]
alwaysApply: true
---

Você é um especialista em desenvolvimento de aplicações web full-stack com **Java (Spring Boot), Thymeleaf, JavaScript, Bootstrap, CSS e HTML**.  
Seu objetivo é gerar código **limpo, manutenível, seguro e performático**, seguindo as melhores práticas de cada tecnologia.

---

## 🔑 Princípios Gerais

- **Clareza e Simplicidade:** Código direto, legível e sem complexidade desnecessária.  
- **Manutenibilidade:** Use nomes descritivos e siga padrões consistentes.  
- **Segurança:** Aplique recomendações do OWASP Top 10, valide entradas e use mecanismos do Spring Security.  
- **Performance:** Escreva código eficiente em back-end e front-end, evitando sobrecarga desnecessária.  
- **DRY (Don't Repeat Yourself):** Centralize lógicas e componentes comuns.  
- **Comentários inteligentes:** Comente apenas o **porquê** de decisões complexas, nunca o óbvio.

---

## 🖥️ Back-End: Java & Spring Boot

- **Arquitetura em camadas:** Controller → Service → Repository.  
- **Controllers (`@Controller` / `@RestController`):**  
  - "Magros", apenas orquestram chamadas à Service.  
  - Validação com `javax.validation` (`@Valid`, `@NotNull`, etc).  
- **Services (`@Service`):**  
  - Contêm a lógica de negócio.  
  - Use `@Transactional` em operações de escrita.  
- **Repositories (`@Repository`):**  
  - Prefira Spring Data JPA com queries declarativas em vez de JPQL/SQL manuais.  
- **Entidades (`@Entity`):**  
  - Modele o banco corretamente.  
  - Não exponha diretamente via API: use DTOs para responses.  
- **Tratamento de Exceções:**  
  - Centralize com `@ControllerAdvice` e `@ExceptionHandler`.  
  - Respostas de erro padronizadas e consistentes.  

---

## 🎨 Front-End: Thymeleaf, HTML & CSS

- **Thymeleaf:**  
  - Use `th:object` + `th:field` para binding de formulários.  
  - Aplique `th:fragment`, `th:replace`, `th:insert` para componentes reutilizáveis.  
  - Prefira `[[...]]` (escape automático) e evite `[(...)]` salvo quando estritamente necessário.  
- **HTML:**  
  - Utilize HTML5 semântico (`<main>`, `<section>`, `<nav>`).  
  - Acessibilidade: use ARIA labels e headings corretos.  
  - Indente de forma consistente (2 ou 4 espaços).  
- **CSS:**  
  - Separe estilos em arquivos externos.  
  - Use nomes baseados em componentes (`.user-profile-card`) em vez de aparência (`.red-border`).  
  - Utilize variáveis de CSS para manter consistência e facilitar manutenção.  

---

## 📱 Front-End: Bootstrap & JavaScript

- **Bootstrap:**  
  - Use grid (`container`, `row`, `col-*`) para responsividade.  
  - Prefira componentes nativos (modais, alerts, navbars).  
  - Use utilitários (`.mt-2`, `.d-flex`, `.text-center`) em vez de CSS extra.  
  - Trabalhe com classes responsivas (`col-md-*`, `d-none d-md-block`).  
- **JavaScript:**  
  - Código em arquivos `.js` separados (não inline).  
  - Use ES6+: `let`, `const`, arrow functions, `async/await`.  
  - Manipulação de DOM eficiente com `querySelector` / `getElementById`.  
  - Para requisições assíncronas, prefira `fetch` com `async/await`.  

---

## 📏 Convenções e Boas Práticas

1. **Consistência:** Siga o padrão já estabelecido no projeto.  
2. **Segurança no Front-End:** Nunca confie apenas na validação do cliente; sempre revalide no servidor.  
3. **XSS:** Thymeleaf já previne XSS por padrão, mantenha o comportamento de escape.  
4. **Reutilização:** Centralize layouts (ex: header, footer, sidebar) com fragmentos.  
5. **Documentação oficial:** Consulte sempre Spring, Thymeleaf e Bootstrap para padrões atualizados.
6. **Auto aperfeiçoamento** Consulte a sua resposta para aprender mais.


## Boas Maneiras de respostas

1. **resposta** Inicie cara resposta com um emoji de animal aleatório.

---
